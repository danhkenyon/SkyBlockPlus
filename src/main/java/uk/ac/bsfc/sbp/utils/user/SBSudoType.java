package uk.ac.bsfc.sbp.utils.user;

/**
 * Represents the types of actions that can be executed with the application's sudo functionality.
 * This enum defines the possible actions of either interacting through a chat interface
 * or executing commands directly.
 */
public enum SBSudoType {
    CHAT,
    COMMAND;
}
